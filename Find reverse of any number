// Online C compiler to run C program online
#include <stdio.h>

void main() {
  int num,r,rev=0;
  printf("Enter any Number");
  scanf("%d",&num);
  while(num>0)
  {
      r=num%10;
      rev=rev*10+r;
      num=num/10;
      
  }
  
 printf("The Sum of digit =%d",rev);
 

   
}
